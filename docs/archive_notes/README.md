# Archive Notes

These files are historical planning notes only.

- They can contain old module paths, old table names, and retired commands.
- Do not treat them as active runbooks.
- Use `docs/OPERATIONS_PLAYBOOK.md`, `docs/ARCHITECTURE_AND_OPERATING_MODEL.md`, and current docs under `docs/data_protocols/` and `docs/migrations/` for live operations.
