# Phase 0A Evidence - Automation Freeze and Lock Validation

Timestamp (UTC): 2026-04-12T04:45:39Z  
Plan reference: `docs/operations/FULL_CLOUD_COMPUTE_CUTOVER_PLAN.md` -> Phase 0, Step 0A

## Environment

- GCP project: `project-4e18de12-63a3-4206-aaa`
- Region for Cloud Run jobs: `us-east4`

## Step 0A Check 1 - Pause refresh-related schedulers

Command used (global scan):

```bash
for loc in $(gcloud scheduler locations list --format='value(locationId)'); do
  gcloud scheduler jobs list --location="$loc" --format="csv[no-heading](name,state,schedule,timeZone)" 2>/dev/null \
  | awk -v l="$loc" -F, 'NF{print l","$0}'
done
```

Result:

- No Cloud Scheduler jobs were returned in any scanned location.
- Refresh-related scheduler jobs to pause: `none present`.

## Step 0A Check 2 - No in-flight Cloud Run executions

Command logic:

```bash
for job in ceiora-prod-serve-refresh ceiora-prod-core-weekly ceiora-prod-cold-core ceiora-prod-cpar-build; do
  gcloud run jobs executions list --job "$job" --region us-east4 --format=json \
  | jq -r 'if length==0 then "NO_EXECUTIONS" else map({name:.metadata.name, completionTime:(.status.completionTime // "")}) | map(select(.completionTime=="")) | if length==0 then "NO_IN_FLIGHT" else .[] | "IN_FLIGHT=" + .name end end'
done
```

Results:

- `ceiora-prod-serve-refresh` -> `NO_IN_FLIGHT`
- `ceiora-prod-core-weekly` -> `NO_IN_FLIGHT`
- `ceiora-prod-cold-core` -> `NO_EXECUTIONS`
- `ceiora-prod-cpar-build` -> `NO_IN_FLIGHT`

## Step 0A Check 3 - Persisted refresh lock state clear

Command:

```bash
OP_TOKEN="$(gcloud secrets versions access latest --secret=ceiora-prod-operator-api-token)"
curl -sS -H "X-Operator-Token: ${OP_TOKEN}" https://control.ceiora.com/api/refresh/status | jq '{status, pipeline_run_id, profile, dispatch_backend, dispatch_id, started_at, finished_at, error}'
```

Result payload:

```json
{
  "status": "ok",
  "pipeline_run_id": null,
  "profile": null,
  "dispatch_backend": null,
  "dispatch_id": null,
  "started_at": null,
  "finished_at": null,
  "error": null
}
```

Interpretation:

- No active refresh run id.
- No stale running status.
- Refresh lock is clear.

## Step 0A Gate Conclusion

Phase 0 Step 0A gate is satisfied:

- automation paused/not active (`no scheduler jobs found`),
- no in-flight Cloud Run executions for required jobs,
- persisted refresh lock state clear.
