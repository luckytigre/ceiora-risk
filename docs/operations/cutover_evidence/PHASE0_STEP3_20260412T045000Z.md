# Phase 0 Evidence - Step 3 Auth/Secret/Identity Preflight

Timestamp (UTC): 2026-04-12T04:50:00Z  
Plan reference: `docs/operations/FULL_CLOUD_COMPUTE_CUTOVER_PLAN.md` -> Phase 0, Step 3

## Auth and identity readiness

- Active gcloud account: `shaun.skc@gmail.com`
- Active GCP project: `project-4e18de12-63a3-4206-aaa`
- Required secrets are readable by operator session:
- `ceiora-prod-operator-api-token` (non-empty)
- `ceiora-prod-editor-api-token` (non-empty)
- `ceiora-prod-neon-database-url` (non-empty)

Measured non-empty lengths:

- `operator_token_len=80`
- `editor_token_len=80`
- `neon_url_len=142`

## Positive and negative token tests

Control endpoint tested: `https://control.ceiora.com`

`/api/operator/status`:

- tokened with `X-Operator-Token` -> `200`
- anonymous -> `401`
- invalid token -> `401`

`/api/refresh/status`:

- tokened with `X-Operator-Token` -> `200`
- anonymous -> `401`
- invalid token -> `401`

These satisfy the Step 3 auth behavior requirement.

## Service-account IAM on Secret Manager

Secret accessor membership checks (`roles/secretmanager.secretAccessor`):

- `ceiora-prod-neon-database-url`:
- `ceioraprodcontrol@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`
- `ceioraprodjobs@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`
- `ceioraprodserve@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`

- `ceiora-prod-operator-api-token`:
- `ceioraprodcontrol@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`
- `ceioraprodjobs@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`
- `ceioraprodserve@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`

- `ceiora-prod-editor-api-token`:
- `ceioraprodserve@project-4e18de12-63a3-4206-aaa.iam.gserviceaccount.com`

Interpretation:

- serve/control/jobs each have Secret Manager access to the NEON DSN secret path.
- operator-token path is available to control/jobs/serve for expected operator/auth behavior.

## Mounted secret refs and version IDs

Active service/job templates mount these secret refs (Cloud Run `secretKeyRef`):

- `service_serve`:
- `ceiora-prod-editor-api-token` key=`latest`
- `ceiora-prod-neon-database-url` key=`latest`
- `ceiora-prod-operator-api-token` key=`latest`

- `service_control`:
- `ceiora-prod-neon-database-url` key=`latest`
- `ceiora-prod-operator-api-token` key=`latest`

- `job_serve_refresh`, `job_core_weekly`, `job_cold_core`, `job_cpar_build`:
- `ceiora-prod-neon-database-url` key=`latest`

Current resolved Secret Manager version IDs (latest enabled):

- `ceiora-prod-editor-api-token` -> version `1`
- `ceiora-prod-neon-database-url` -> version `1`
- `ceiora-prod-operator-api-token` -> version `1`

## Step 3 Gate Conclusion

Phase 0 Step 3 is satisfied:

- auth session valid,
- required secrets readable,
- positive and negative token auth checks passed,
- service-account secret access path verified,
- mounted secret refs and current version IDs recorded.
