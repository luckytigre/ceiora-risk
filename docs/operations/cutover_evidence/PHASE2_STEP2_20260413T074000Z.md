# Phase 2 Evidence - Cloud Compute Wave (cUSE + cPAR)

Timestamp (UTC): 2026-04-13T07:40:00Z  
Plan reference: `docs/operations/FULL_CLOUD_COMPUTE_CUTOVER_PLAN.md` -> Phase 2

## Summary

- cUSE cloud `cold-core` wave completed successfully (`7/7`) before this phase capture.
- Initial cloud `cpar-weekly` dispatch failed due stale source proxy anchor-week prices.
- Local-ingest source refresh + Neon source-sync advanced source prices to `2026-04-10`.
- Cloud cPAR dispatch then succeeded, including explicit package-date publish at `2026-04-10`.

## 1) cUSE cloud lane status (from prior step)

- Control refresh status:
  - `profile=cold-core`
  - `pipeline_run_id=crj_5fa1bf2dd46e`
  - `dispatch_id=ceiora-prod-cold-core-wv55g`
  - `status=ok`
  - `finished_at=2026-04-13T05:55:00.617781+00:00`

## 2) cPAR weekly cloud dispatch and failure capture

Dispatch:

- `POST /api/cpar/build?profile=cpar-weekly`
- response:
  - `pipeline_run_id=cpar_crj_ecd311a4f846`
  - `package_date=2026-04-10`
  - `dispatch_backend=cloud_run_job`

Execution result:

- Cloud Run execution `ceiora-prod-cpar-build-g5c2p`
- terminal: failed (`exit code 1`)

Reproduction against Neon showed root cause:

- `run_cpar_pipeline --profile cpar-weekly --as-of-date 2026-04-10`
- failed in `source_read` with:
  - `Missing anchor-week price for factor proxies: IWM, MTUM, QUAL, SPY, USMV, VLUE, XLB, XLC, XLE, XLF, XLI, XLK, XLP, XLRE, XLU, XLV, XLY`

## 3) Source refresh/sync to unblock cPAR

Command lane (local-ingest contract):

- `ORCHESTRATOR_ENABLE_INGEST=true`
- `APP_RUNTIME_ROLE=local-ingest`
- `DATA_BACKEND=neon`
- `NEON_AUTHORITATIVE_REBUILDS=true`
- `run_model_pipeline --profile source-daily --as-of-date 2026-04-10 --from-stage ingest --to-stage source_sync`

Observed ingest outputs:

- universe processed: `5565`
- `price_rows_inserted=3500`
- `price_gap_repair.rows_upserted=31487`
- repaired window: `2026-03-27 -> 2026-04-10`

Observed source-sync outputs:

- stage `source_sync` status: `completed`
- sync run id: `source_sync_20260413T072801930544Z_14ce53d5`
- source prices advanced:
  - before sync: `prices_asof=2026-03-26`
  - after sync input: `prices_asof=2026-04-10`

Note on lane terminal status:

- overall lane returned failed due `neon_mirror=mismatch` parity policy, but source-sync completed and advanced source prices as required for cPAR proxy coverage.

## 4) cPAR cloud reruns after source-sync

### 4A. Cloud `cpar-weekly` rerun

Dispatch:

- `POST /api/cpar/build?profile=cpar-weekly`
- `pipeline_run_id=cpar_crj_ca0f7c23a746`

Execution:

- Cloud Run execution `ceiora-prod-cpar-build-775h8`
- terminal: success (`succeededCount=1`)

Resulting cPAR meta at that point:

- `package_run_id=cpar_crj_ca0f7c23a746`
- `package_date=2026-04-02`

### 4B. Cloud explicit package-date publish

Dispatch:

- `POST /api/cpar/build?profile=cpar-package-date&as_of_date=2026-04-10`
- `pipeline_run_id=cpar_crj_0ff4c9a3050b`

Execution:

- Cloud Run execution `ceiora-prod-cpar-build-24f82`
- terminal: success (`succeededCount=1`)

Live read verification:

- `GET /api/cpar/meta` now returns:
  - `package_run_id=cpar_crj_0ff4c9a3050b`
  - `package_date=2026-04-10`
  - `source_prices_asof=2026-04-10`
  - `classification_asof=2026-03-31`
  - `method_version=cPAR1_residual_v1`

## Phase 2 checkpoint state

- cUSE cloud lane: green for this wave.
- cPAR cloud lane: green, with active package pinned to requested `2026-04-10`.
- Remaining open item from this sequence: resolve/ratify `neon_mirror` parity mismatch policy so source lanes can close without a non-critical failed terminal state.
