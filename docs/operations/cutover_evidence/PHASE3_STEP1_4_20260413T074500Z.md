# Phase 3 Evidence - Guardrail/Operator Checks

Timestamp (UTC): 2026-04-13T07:45:00Z  
Plan reference: `docs/operations/FULL_CLOUD_COMPUTE_CUTOVER_PLAN.md` -> Phase 3 (Step 1-4)

## Executed checks

1. `make cloud-topology-check` (with operator token)
2. `make operator-check` with:
   - `APP_BASE_URL=https://app.ceiora.com`
   - `CONTROL_BASE_URL=https://control.ceiora.com`
   - `OPERATOR_CHECK_REQUIRE_LIVE=1`
   - `OPERATOR_API_TOKEN=<secret>`

## Results

- Topology/unit suite (`58 passed`) is green.
- Live operator checks are **not green** due one shared failure:
  - proxied health diagnostics expected `200`
  - actual `503` with:
    - `status=not_ready`
    - `error=cache_not_ready`
    - `cache_key=health_diagnostics`
    - message instructs rerun of `core-weekly`/`cold-core`

Observed context bundled in response:

- latest cold-core run is completed and `status=ok`
- serving refresh in that run reports `health_diagnostics` payload persisted in Neon

## Direct endpoint spot checks

- `GET https://ceiora-prod-control-.../api/health/diagnostics` (tokened): `503 cache_not_ready`
- `GET https://ceiora-prod-serve-.../api/health/diagnostics` (tokened): `503 cache_not_ready`
- `GET https://app.ceiora.com/api/health/diagnostics` (tokened): `404` (edge/proxy path does not expose this route directly)

## Phase 3 status

- Phase 3 remains **blocked** on cloud diagnostics-read behavior.
- Next remediation slice:
  - fix diagnostics-read path so cloud surfaces serve persisted diagnostics correctly when available (Neon-authoritative path),
  - then rerun `cloud-topology-check` and `operator-check`.
