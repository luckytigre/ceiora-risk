# Backend API target for Next.js rewrite proxy.
# Use your deployed backend URL in production.
BACKEND_API_ORIGIN=http://localhost:8000

# Optional: secret used by frontend server routes
# (/api/refresh and /api/refresh/status) when backend refresh auth is enabled.
REFRESH_API_TOKEN=
